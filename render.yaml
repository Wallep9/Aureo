services:
  - type: web
    name: aureo
    env: python
    plan: free
    buildCommand: echo "Sin dependencias — solo Python stdlib"
    startCommand: python server.py
    envVars:
      - key: PORT
        generateValue: false
